spring:
  application:
    name: bacao
  liquibase:
#    change-log: db/changelog/db.changelog-master.yaml
    enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/bacao_db
    username: admin
    password: root

server:
  port: 8082

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true